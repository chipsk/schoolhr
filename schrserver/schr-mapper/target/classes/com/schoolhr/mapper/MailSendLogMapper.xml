<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.schoolhr.mapper.MailSendLogMapper">
    <insert id="insert">
        insert into mail_send_log (msgId,empId,routeKey,exchange,tryTime,createTime) values (#{msgId},#{empId},#{routeKey},#{exchange},#{tryTime},#{createTime});
    </insert>
    <update id="updateMailSendLogStatus"></update>
    <update id="updateCount"></update>
    <select id="getMailSendLogsByStatus" resultType="com.schoolhr.model.MailSendLog"></select>
</mapper>